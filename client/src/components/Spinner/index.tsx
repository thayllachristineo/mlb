import { FC } from 'react';

import * as Styled from './Spinner.syled';

const Spinner: FC = (): JSX.Element => (
  <Styled.Wrapper>
    <Styled.Content />
  </Styled.Wrapper>
);

export default Spinner;
